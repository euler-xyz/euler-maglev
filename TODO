NEXT
  ! test graceful degrade when pricing API not available
  * links to spy mode
  ? when hovering, show an overlay line
  ? fix hardcoded gas limits (foundry problem?)
  ? support manually entering reserves

TODO
  * each sub-account tab should get its own URL route
  * spy mode
  * figure out better solution for RPC URLs
  * show net APY, Return on Equity, native yield (?)
